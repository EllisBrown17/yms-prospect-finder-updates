// V5.0.2 intelligence workspace UX overrides
let v502ProductSearch='',v502ProductDivision='All';
const v502ActionStages=['replied','follow_up_due','needs_approval','ready_to_send','ready_to_draft','needs_contact','review'];
const v502StageOrder={replied:0,follow_up_due:1,needs_approval:2,ready_to_send:3,ready_to_draft:4,needs_contact:5,review:6,needs_research:20,sent_waiting:30,qualified:31,deferred:32,closed:40,suppressed:41};
function v502StageLabel(s){return s==='action'?'Action needed':v5StageLabel(s)}
function v502ModelChips(models,limit=4){models=models||[];let shown=models.slice(0,limit);return shown.map(m=>`<span class="v502Model">${v5e(m)}</span>`).join('')+(models.length>limit?`<span class="v502More">+${models.length-limit} more</span>`:'')}
function v502EnsureProductTools(){
  let root=$('v5ProductCards');if(!root)return;let host=root.parentElement;
  if(!$('v502ProductToolbar')){let bar=document.createElement('div');bar.id='v502ProductToolbar';bar.className='v502Toolbar';bar.innerHTML=`<input id="v502ProductSearch" class="v502Search" placeholder="Search ALFRA product, model or category…" autocomplete="off"><div id="v502ProductFilters" class="v502FilterRow"></div><div class="v502LibraryMeta"><b id="v502ProductCount">0</b><span>product families · current models are shown inside each family</span></div>`;host.insertBefore(bar,root);$('v502ProductSearch').addEventListener('input',e=>{v502ProductSearch=(e.target.value||'').toLowerCase().trim();v502RenderProductLibrary()})}
  let divs=['All',...new Set(v5Catalog.map(p=>p.division||'Other'))];$('v502ProductFilters').innerHTML=divs.map(d=>`<button class="v502Filter ${d===v502ProductDivision?'active':''}" onclick="v502ProductDivision=${JSON.stringify(d)};v502RenderProductLibrary()">${v5e(d)}</button>`).join('')
}
function v502RenderProductLibrary(){v502EnsureProductTools();let root=$('v5ProductCards');if(!root)return;let q=v502ProductSearch;let rows=v5Catalog.filter(p=>{let division=p.division||'Other';if(v502ProductDivision!=='All'&&division!==v502ProductDivision)return false;if(!q)return true;return [p.name,p.category,p.division,...(p.models||[])].join(' ').toLowerCase().includes(q)});if($('v502ProductCount'))$('v502ProductCount').textContent=rows.length;if(!rows.length){root.innerHTML='<div class="v502Empty">No ALFRA products match that search.</div>';return}root.innerHTML=rows.map(p=>`<div class="v5Product ${p.id===v5SelectedProduct?'active':''}" onclick="selectV5Product('${v5e(p.id)}')"><div class="v502Division">${v5e(p.division||'ALFRA')} · ${v5e(p.category||'')}</div><div class="verify">✓ VERIFIED PRODUCT FAMILY</div><h3>${v5e(p.name)}</h3><p>${v5e(p.verified_fact)}</p><div class="v502Models">${v502ModelChips(p.models,4)}</div></div>`).join('')}
loadProducts=async function(){await loadV5Catalog();if(!v5Catalog.find(p=>p.id===v5SelectedProduct)&&v5Catalog[0])v5SelectedProduct=v5Catalog[0].id;v502RenderProductLibrary();paintProductDetail();await loadProductOpportunities()};
selectV5Product=async function(id){v5SelectedProduct=id;v502RenderProductLibrary();paintProductDetail();await loadProductOpportunities()};
paintProductDetail=function(){let p=v5Catalog.find(x=>x.id===v5SelectedProduct),root=$('v5ProductDetail');if(!p||!root)return;let models=p.models||[],roles=p.likely_roles||[],qs=p.validation_questions||[],flow=p.workflow||[];root.innerHTML=`<div class="v4SectionHead"><div><div class="ey">PRODUCT BRIEFING</div><h2>${v5e(p.name)}</h2><p>${v5e(p.division||'ALFRA')} · ${v5e(p.category)} · verified product family</p></div><a class="btn" href="${v5e(p.source_url)}" target="_blank" rel="noopener">Official product info</a></div><div class="v502AllModels">${models.map(m=>`<span>${v5e(m)}</span>`).join('')}</div><div class="v502DetailGrid"><div class="v502DetailBox"><div class="lab">What it is</div><p>${v5e(p.verified_fact)}${(p.verified_details||[]).length?`<br><br>${(p.verified_details||[]).map(x=>'• '+v5e(x)).join('<br>')}`:''}</p></div><div class="v502DetailBox"><div class="lab">Where it fits</div><p>${v5e(p.simple||'')}</p><div class="v5Flow" style="margin-top:10px">${flow.map((x,i)=>`${i?'<i>→</i>':''}<span>${v5e(x)}</span>`).join('')}</div></div><div class="v502DetailBox"><div class="lab">Who to talk to</div><div class="v502RoleRow">${roles.map(x=>`<span class="v502Role">${v5e(x)}</span>`).join('')||'<span class="muted small">Validate the buying role.</span>'}</div></div><div class="v502DetailBox"><div class="lab">Questions to ask</div><ol class="v502Questions">${qs.slice(0,6).map(q=>`<li>${v5e(q)}</li>`).join('')}</ol></div></div><div class="notice"><b>Do not claim:</b> ${v5e(p.do_not_claim||'Validate the application before claiming technical suitability.')}</div>`};
renderProductOpportunities=function(){let p=v5Catalog.find(x=>x.id===v5SelectedProduct);if($('v5OppTitle'))$('v5OppTitle').textContent=`${p?p.name:'Product'} opportunities · ${v5ProductRows.length}`;let root=$('v5OpportunityList');if(!root)return;let card=root.closest('.card');if(card&&!card.querySelector('.v502ScoreHelp')){let h=card.querySelector('.v4SectionHead');if(h){let x=document.createElement('div');x.className='v502ScoreHelp';x.textContent='Priority score = strength of product/company evidence, not probability of purchase.';h.after(x)}}if(!v5ProductRows.length){root.innerHTML='<div class="v502Empty">No prospects currently meet this evidence threshold. Lower the threshold or run Smart Finder / Smart Pipeline.</div>';return}root.innerHTML=v5ProductRows.slice(0,300).map(r=>{let o=r.opportunity||{},learn=o.learning||{},why=(o.observed||[])[0]?.text||((o.matched_signals||{}).strong||[]).join(', ')||'Weak evidence — validate first';return `<div class="v5Opportunity"><div><strong>${v5e(r.company)}</strong><div class="muted">${v5e(r.sector||'')}${r.address?' · '+v5e(r.address):''}</div></div><div class="v5Score">${o.priority_score||0}<small>/100</small><div class="muted">fit ${o.fit_score||0}</div></div><div><span class="v5Tag ${String(o.confidence||'').toLowerCase()}">${v5e(o.confidence||'Validate')}</span><div class="muted">confidence</div></div><div class="v5Why"><b>${v5e(why)}</b><div class="muted">${learn.sample?`${learn.sample} logged sales outcomes · ${v5e(learn.confidence)} learning confidence`:'No sales-history adjustment yet'}</div></div><div style="display:flex;gap:5px;flex-wrap:wrap"><button class="btn" onclick="openD('${r.id}')">Open</button><button class="btn primary" onclick="openOutreachFor('${r.id}')">Email</button></div></div>`}).join('')};
function v502StageCount(sum,s){let st=sum.stages||{};if(s==='action')return v502ActionStages.reduce((n,k)=>n+Number(st[k]||0),0);if(s==='all')return Number(sum.total||0);return Number(st[s]||0)}
function v502StageButton(sum,s){return `<button class="outStage ${s===v5OutStage?'active':''}" onclick="setOutStage('${s}')">${v5e(v502StageLabel(s))}<span class="count">${v502StageCount(sum,s)}</span></button>`}
loadOutreach=async function(){await loadV5Catalog();let sum=await api('/api/outreach/summary');if($('outKTotal'))$('outKTotal').textContent=sum.total||0;if($('outKApproval'))$('outKApproval').textContent=sum.needs_approval||0;if($('outKFollow'))$('outKFollow').textContent=sum.follow_up_due||0;if($('outKReplies'))$('outKReplies').textContent=sum.replied||0;if($('outKQual'))$('outKQual').textContent=sum.qualified||0;if($('outKSent'))$('outKSent').textContent=sum.sent_total||0;let primary=['action','replied','follow_up_due','needs_approval','ready_to_send','ready_to_draft','needs_contact'];let secondary=['needs_research','sent_waiting','review','qualified','deferred','closed','suppressed','all'];if($('outStageBar'))$('outStageBar').innerHTML=`<div class="v502StageHeading"><div><b>What needs attention</b><div class="v502QueueIntro">Replies and follow-ups first, then approvals, drafts and missing contacts.</div></div><div class="v502Attention"><span class="v502Dot"></span>${v502StageCount(sum,'action')} actionable</div></div><div class="v502StageGroup">${primary.map(s=>v502StageButton(sum,s)).join('')}</div><div class="v502SecondaryStages"><div class="v502StageGroup">${secondary.map(s=>v502StageButton(sum,s)).join('')}</div></div>`;let backendStage=v5OutStage==='action'?'all':v5OutStage;v5OutRows=await api(`/api/outreach/queue?stage=${encodeURIComponent(backendStage)}`);if(v5OutStage==='action')v5OutRows=v5OutRows.filter(r=>v502ActionStages.includes((r.state||{}).stage));v5OutRows.sort((a,b)=>{let as=(a.state||{}).stage,bs=(b.state||{}).stage;let ao=v502StageOrder[as]??50,bo=v502StageOrder[bs]??50;if(ao!==bo)return ao-bo;return Number((b.product||{}).priority_score||0)-Number((a.product||{}).priority_score||0)});let title=document.querySelector('#outreach .outreachQueue h2');if(title)title.textContent=v502StageLabel(v5OutStage)+' · '+v5OutRows.length;renderOutreachQueue();if(v5OutSelected){let r=v5OutRows.find(x=>x.id===v5OutSelected);if(r)paintOutWorkbench(r)}};
setOutStage=async function(s){v5OutStage=s;v5OutSelected='';await loadOutreach()};
renderOutreachQueue=function(){let root=$('outQueue');if(!root)return;let q=($('outSearch')?.value||'').toLowerCase().trim(),arr=v5OutRows.filter(r=>!q||[r.company,r.sector,r.email,(r.product||{}).product_name].join(' ').toLowerCase().includes(q));if(!arr.length){root.innerHTML='<div class="v502Empty">Nothing in this queue. Choose another stage or run more prospect research.</div>';return}root.innerHTML=arr.slice(0,400).map(r=>{let st=r.state||{},p=r.product||{},selected=r.id===v5OutSelected;let follow=st.next_followup?`Follow-up ${v5e(st.next_followup)}`:'';return `<div class="outRow ${selected?'active':''}" onclick="selectOutreach('${r.id}')"><div><b>${v5e(r.company)}</b><small>${v5e(r.sector||'Uncategorised')}</small><div class="v502CompanyMeta"><span class="v502ProductPill">${v5e(p.product_name||'No product match yet')}</span></div><div class="v502Email">${v5e(r.email||'No email found yet')}</div></div><div><b class="v502QueueScore">${p.priority_score||0}<small>/100</small></b><small>product-fit priority</small><span class="outStatus ${v5e(st.stage)}">${v5e(v5StageLabel(st.stage))}</span><small>${st.attempts||0} logged send${Number(st.attempts||0)===1?'':'s'}${follow?' · '+follow:''}</small></div><div><div class="v502QueueActions"><button class="btn" onclick="event.stopPropagation();openD('${r.id}')">Company</button><button class="btn primary" onclick="event.stopPropagation();selectOutreach('${r.id}');$('outWorkbench')?.scrollIntoView({behavior:'smooth',block:'start'})">Work email</button></div></div></div>`}).join('')};
paintOutWorkbench=function(r){let root=$('outWorkbench');if(!root)return;let d=r.draft||{},p=r.product||{},st=r.state||{},hist=r.history||[];let prodOpts=v5Catalog.map(x=>`<option value="${x.id}" ${(d.product_id||p.product_id)===x.id?'selected':''}>${v5e(x.name)}</option>`).join('');let outcomeOpts=Object.entries({qualified_opportunity:'Qualified opportunity',not_relevant:'Not relevant',already_has_equipment:'Already has equipment',buys_pre_cut:'Buys pre-cut / pre-processed',outsourced:'Process outsourced',low_volume:'Volume too low',deferred:'Deferred / revisit later',won:'Won',lost:'Lost',no_reply:'No reply',do_not_contact:'Do not contact'}).map(([k,v])=>`<option value="${k}">${v}</option>`).join('');root.innerHTML=`<div class="v4SectionHead"><div><div class="ey">WORK THIS PROSPECT</div><h2 style="margin:3px 0">${v5e(r.company)}</h2><p>${v5e(v5StageLabel(st.stage))} · ${(st.attempts||0)} logged send(s)${p.product_name?' · '+v5e(p.product_name):''}</p></div><button class="btn" onclick="openD('${r.id}')">Full company</button></div><div class="v502DetailBox"><div class="lab">1 · Contact</div><div class="row3"><div class="field"><label>Name</label><input id="owName" value="${v5e(d.contact_name||r.contact_name||'')}"></div><div class="field"><label>Role</label><input id="owRole" value="${v5e(d.contact_role||r.contact_role||'')}"></div><div class="field"><label>Email</label><input id="owEmail" value="${v5e(d.email||r.email||'')}"></div></div></div><div class="v502DetailBox" style="margin-top:10px"><div class="lab">2 · Email angle & draft</div><div class="row2"><div class="field"><label>Primary ALFRA angle</label><select id="owProduct">${prodOpts}</select></div><div class="field"><label>Email type</label><select id="owEmailType"><option value="first_contact">First contact</option><option value="follow_up">Follow-up</option><option value="reengagement">Re-engagement</option><option value="other">Other</option></select></div></div><div class="field"><label>Subject</label><input id="owSubject" value="${v5e(d.subject||'')}"></div><div class="field"><label>Email</label><textarea id="owBody">${v5e(d.body||'')}</textarea></div><div class="workbenchActions"><button class="btn primary" onclick="generateOutreachDraft('${r.id}')">Generate grounded draft</button><button class="btn" onclick="approveOutreachDraft('${r.id}')">Approve / save edit</button><button class="btn" onclick="openDraftInMail('${r.id}')">Open in email</button></div></div><div class="v502DetailBox" style="margin-top:10px"><div class="lab">3 · Send & remember</div><div class="row2"><div class="field"><label>Sent date (optional for backfill)</label><input type="date" id="owSentAt"></div><div class="field"><label>Next follow-up</label><input type="date" id="owFollow" value="${v5e(st.next_followup||'')}"></div></div><button class="btn primary" onclick="markOutreachSent('${r.id}')">Mark sent & add to history</button><div class="v502QueueIntro">Opening your email app does not count as sent. Mark sent only after the message has genuinely gone.</div></div><div class="v502DetailGrid"><div class="v502DetailBox"><div class="lab">Reply / note</div><textarea id="owNote" style="min-height:78px" placeholder="Paste or summarise what they said…"></textarea><div class="v502QueueActions"><button class="btn" onclick="logOutreachReply('${r.id}')">Log reply</button><button class="btn" onclick="logOutreachNote('${r.id}')">Add note</button></div></div><div class="v502DetailBox"><div class="lab">Sales outcome</div><select id="owOutcome">${outcomeOpts}</select><textarea id="owOutcomeNote" style="min-height:78px;margin-top:7px" placeholder="Why? e.g. all busbar arrives pre-cut"></textarea><button class="btn" style="margin-top:7px" onclick="logOutreachOutcome('${r.id}')">Save outcome</button></div></div><details class="v4Details" ${hist.length?'open':''}><summary>Outreach timeline (${hist.length})</summary><div>${hist.length?hist.map(h=>`<div class="historyItem"><b>${v5e((h.type||'').toUpperCase())}${h.email_type?` · ${v5e(h.email_type.replaceAll('_',' '))}`:''}${h.outcome?` · ${v5e(h.outcome.replaceAll('_',' '))}`:''}</b> <small>${v5e(h.at||'')}</small><div>${v5e(h.email||h.contact_name||h.note||'')}</div>${h.subject?`<div class="muted">${v5e(h.subject)}</div>`:''}</div>`).join(''):'<div class="muted small">No outreach logged yet.</div>'}</div></details>`};
if(v5OutStage==='all')v5OutStage='action';
